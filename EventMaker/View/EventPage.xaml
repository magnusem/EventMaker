<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EventMaker.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ViewModel="using:EventMaker.ViewModel"
    xmlns:Interactivity="using:Microsoft.Xaml.Interactivity" xmlns:Core="using:Microsoft.Xaml.Interactions.Core"
    x:Class="EventMaker.View.EventPage"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">


        <ListView x:Name="listView1" HorizontalAlignment="Left" Height="487" Margin="103,111,0,0" VerticalAlignment="Top" Width="283" ItemsSource="{Binding MinEventCataLogSingleton.EventListe}">
            <ListView.DataContext>
                <ViewModel:EventViewModel/>
            </ListView.DataContext>
        </ListView>
        <AppBarButton x:Name="appBarButton" HorizontalAlignment="Left" Icon="Accept" Label="Create Event" Margin="753,207,0,0" VerticalAlignment="Top" Height="118" Width="162" d:LayoutOverrides="VerticalAlignment">
            <Interactivity:Interaction.Behaviors>
                <Core:EventTriggerBehavior EventName="Click">
                    <Core:NavigateToPageAction TargetPage="EventMaker.View.CreateEventPage"/>
                </Core:EventTriggerBehavior>
            </Interactivity:Interaction.Behaviors>
        </AppBarButton>
        <TextBlock x:Name="textBlock" HorizontalAlignment="Left" Margin="605,499,0,0" TextWrapping="Wrap" Text="{Binding selectedEvent}" VerticalAlignment="Top" Height="99" Width="477">
            <TextBlock.DataContext>
                <ViewModel:EventViewModel/>
            </TextBlock.DataContext>
        </TextBlock>

    </Grid>
</Page>
